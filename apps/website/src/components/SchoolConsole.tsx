import React, { useEffect, useState } from 'react';
import { ArrowLeft, BarChart3, Building2, CheckCircle2, GraduationCap, Landmark, RefreshCw, ShieldCheck, Users, WalletCards } from 'lucide-react';

interface Props { onBackToHome: () => void; }
interface Overview { school?: { legalName?: string; businessTag?: string }; counts: { campuses: number; classes: number; students: number; staff: number }; fees: Array<{ name: string; amount: string; currency: string }>; savingsPolicies: Array<{ name: string; percentage: string; currency: string; status: string }>; }

const API_BASE_URL = String((import.meta as any).env?.VITE_API_BASE_URL || 'http://localhost:4000').replace(/\/$/, '');

export const SchoolConsole: React.FC<Props> = ({ onBackToHome }) => {
  const [overview, setOverview] = useState<Overview | null>(null);
  const [loading, setLoading] = useState(false);
  const [message, setMessage] = useState('Ready to create a school workspace.');
  const [onboarding, setOnboarding] = useState({ schoolLegalName: '', registrationNumber: '', adminName: '', adminEmail: '', adminPhone: '', country: 'NG', industry: 'education', description: '', incorporationDate: '', address: '', city: '', state: '', postalCode: '', website: '', annualRevenue: '', estimatedMonthlyDeposits: '', estimatedMonthlyWithdrawals: '', sourceOfFunds: '', taxId: '', taxIdType: 'TIN', adminDateOfBirth: '', adminNationality: 'NG', adminGender: 'other', adminIdType: 'national_id', adminIdNumber: '', adminIdFrontUrl: '', adminIdBackUrl: '', proofOfAddressUrl: '', registrationDocumentUrl: '', adminBvn: '', adminNin: '' });
  const [onboardingComplete, setOnboardingComplete] = useState(false);
  const entityId = typeof window !== 'undefined' ? new URLSearchParams(window.location.search).get('entityId') : null;

  const loadOverview = async () => {
    if (!entityId) { setMessage('Ready to create a school workspace.'); return; }
    setLoading(true);
    try {
      const response = await fetch(`${API_BASE_URL}/api/schools/${encodeURIComponent(entityId)}/overview`);
      const data = await response.json();
      if (!response.ok) throw new Error(data.error || 'Unable to connect school workspace.');
      setOverview(data);
      setMessage('Live school records connected.');
    } catch (error: any) { setMessage(error.message || 'Unable to connect school workspace.'); }
    finally { setLoading(false); }
  };

  useEffect(() => { loadOverview(); }, [entityId]);

  const startOnboarding = async (event: React.FormEvent) => {
    event.preventDefault();
    setLoading(true);
    try {
      const response = await fetch(`${API_BASE_URL}/api/schools/applications`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(onboarding) });
      const data = await response.json();
      if (!response.ok) throw new Error(data.error || 'Unable to submit school KYB application.');
      setOnboardingComplete(true);
      setMessage('KYB application submitted. Our team will review the school and authorized administrator.');
    } catch (error: any) { setMessage(error.message || 'Unable to submit school KYB application.'); }
    finally { setLoading(false); }
  };

  const metrics = overview ? [
    { label: 'Students', value: overview.counts.students, icon: <Users size={18} />, tone: 'text-cyan-300' },
    { label: 'Classes', value: overview.counts.classes, icon: <GraduationCap size={18} />, tone: 'text-amber-300' },
    { label: 'Campuses', value: overview.counts.campuses, icon: <Building2 size={18} />, tone: 'text-emerald-300' },
    { label: 'Staff', value: overview.counts.staff, icon: <ShieldCheck size={18} />, tone: 'text-pink-300' },
  ] : [];

  return <div className="min-h-screen bg-[#071018] text-[#F7F8F4]">
    <header className="border-b border-white/10 bg-[#09141d]/90 px-6 py-4">
      <div className="mx-auto flex max-w-7xl items-center justify-between gap-4">
        <div className="flex items-center gap-3"><button onClick={onBackToHome} className="rounded-xl border border-white/10 bg-white/5 p-2 text-white/70 hover:text-white" aria-label="Back to Proxim"><ArrowLeft size={18} /></button><div><div className="flex items-center gap-2 text-lg font-bold"><Landmark size={19} className="text-cyan-300" /> School Console</div><div className="text-xs text-white/45">Collections and treasury operations</div></div></div>
        <button onClick={loadOverview} className="flex items-center gap-2 rounded-xl border border-cyan-300/25 bg-cyan-300/10 px-3 py-2 text-sm font-semibold text-cyan-200 hover:bg-cyan-300/20"><RefreshCw size={15} className={loading ? 'animate-spin' : ''} /> Refresh</button>
      </div>
    </header>
    <main className="mx-auto max-w-7xl px-6 py-10">
      <div className="mb-10 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><div className="mb-3 text-xs font-bold uppercase tracking-[0.2em] text-cyan-300">School operations</div><h1 className="text-4xl font-bold tracking-tight md:text-5xl">{overview?.school?.legalName || 'Create your school workspace'}</h1><p className="mt-3 max-w-xl text-white/55">{overview ? 'Manage student collections, fee schedules, reserves, payroll readiness, and financial reporting in one place.' : 'Start with school verification, then configure accounts, classes, students, fees, payroll, and reserves.'}</p></div><div className="rounded-2xl border border-white/10 bg-white/[0.04] px-4 py-3 text-sm text-white/60"><span className="mr-2 inline-block h-2 w-2 rounded-full bg-amber-300" />{message}</div></div>
      {!overview && !onboardingComplete && <section className="mb-8 max-w-5xl rounded-2xl border border-cyan-300/20 bg-[#0c1b25] p-6"><div className="mb-6 flex items-center gap-3"><ShieldCheck className="text-cyan-300" /><div><h2 className="text-xl font-bold">Step 1: Brails business KYB application</h2><p className="text-sm text-white/45">Complete every section. The same data is saved locally and submitted to Brails for school business verification.</p></div></div><form onSubmit={startOnboarding} className="grid gap-4 sm:grid-cols-2"><div className="sm:col-span-2 text-xs font-bold uppercase tracking-[0.16em] text-cyan-300">School profile</div><input required value={onboarding.schoolLegalName} onChange={event => setOnboarding({ ...onboarding, schoolLegalName: event.target.value })} className="field" placeholder="School legal name" /><input required value={onboarding.registrationNumber} onChange={event => setOnboarding({ ...onboarding, registrationNumber: event.target.value })} className="field" placeholder="Registration number" /><input required value={onboarding.industry} onChange={event => setOnboarding({ ...onboarding, industry: event.target.value })} className="field" placeholder="Industry" /><input required value={onboarding.incorporationDate} onChange={event => setOnboarding({ ...onboarding, incorporationDate: event.target.value })} className="field" type="date" /><textarea required value={onboarding.description} onChange={event => setOnboarding({ ...onboarding, description: event.target.value })} className="field sm:col-span-2" placeholder="Business description and account purpose" /><input value={onboarding.website} onChange={event => setOnboarding({ ...onboarding, website: event.target.value })} className="field" placeholder="School website (optional)" /><select value={onboarding.country} onChange={event => setOnboarding({ ...onboarding, country: event.target.value })} className="field"><option value="NG">Nigeria</option><option value="KE">Kenya</option><option value="UG">Uganda</option></select><div className="sm:col-span-2 text-xs font-bold uppercase tracking-[0.16em] text-cyan-300">Registered address</div><input required value={onboarding.address} onChange={event => setOnboarding({ ...onboarding, address: event.target.value })} className="field sm:col-span-2" placeholder="Street address" /><input required value={onboarding.city} onChange={event => setOnboarding({ ...onboarding, city: event.target.value })} className="field" placeholder="City" /><input required value={onboarding.state} onChange={event => setOnboarding({ ...onboarding, state: event.target.value })} className="field" placeholder="State or province" /><input required value={onboarding.postalCode} onChange={event => setOnboarding({ ...onboarding, postalCode: event.target.value })} className="field" placeholder="Postal code" /><input value={onboarding.sourceOfFunds} onChange={event => setOnboarding({ ...onboarding, sourceOfFunds: event.target.value })} className="field" placeholder="Source of funds" /><div className="sm:col-span-2 text-xs font-bold uppercase tracking-[0.16em] text-cyan-300">Financial and tax profile</div><input required value={onboarding.taxId} onChange={event => setOnboarding({ ...onboarding, taxId: event.target.value })} className="field" placeholder="Tax ID" /><input required value={onboarding.taxIdType} onChange={event => setOnboarding({ ...onboarding, taxIdType: event.target.value })} className="field" placeholder="Tax ID type" /><input value={onboarding.annualRevenue} onChange={event => setOnboarding({ ...onboarding, annualRevenue: event.target.value })} className="field" placeholder="Annual revenue" /><input value={onboarding.estimatedMonthlyDeposits} onChange={event => setOnboarding({ ...onboarding, estimatedMonthlyDeposits: event.target.value })} className="field" placeholder="Estimated monthly deposits" /><input value={onboarding.estimatedMonthlyWithdrawals} onChange={event => setOnboarding({ ...onboarding, estimatedMonthlyWithdrawals: event.target.value })} className="field" placeholder="Estimated monthly withdrawals" /><input required value={onboarding.registrationDocumentUrl} onChange={event => setOnboarding({ ...onboarding, registrationDocumentUrl: event.target.value })} className="field" placeholder="Registration document URL" /><div className="sm:col-span-2 text-xs font-bold uppercase tracking-[0.16em] text-cyan-300">Authorized administrator</div><input required value={onboarding.adminName} onChange={event => setOnboarding({ ...onboarding, adminName: event.target.value })} className="field" placeholder="Full legal name" /><input required type="email" value={onboarding.adminEmail} onChange={event => setOnboarding({ ...onboarding, adminEmail: event.target.value })} className="field" placeholder="Email" /><input required value={onboarding.adminPhone} onChange={event => setOnboarding({ ...onboarding, adminPhone: event.target.value })} className="field" placeholder="Phone" /><input required type="date" value={onboarding.adminDateOfBirth} onChange={event => setOnboarding({ ...onboarding, adminDateOfBirth: event.target.value })} className="field" /><select required value={onboarding.adminGender} onChange={event => setOnboarding({ ...onboarding, adminGender: event.target.value })} className="field"><option value="other">Gender</option><option value="female">Female</option><option value="male">Male</option></select><input required value={onboarding.adminNationality} onChange={event => setOnboarding({ ...onboarding, adminNationality: event.target.value })} className="field" placeholder="Nationality code" /><select required value={onboarding.adminIdType} onChange={event => setOnboarding({ ...onboarding, adminIdType: event.target.value })} className="field"><option value="national_id">National ID</option><option value="passport">Passport</option><option value="drivers_license">Driver license</option><option value="voter_card">Voter card</option></select><input required value={onboarding.adminIdNumber} onChange={event => setOnboarding({ ...onboarding, adminIdNumber: event.target.value })} className="field" placeholder="Identity document number" /><input required value={onboarding.adminIdFrontUrl} onChange={event => setOnboarding({ ...onboarding, adminIdFrontUrl: event.target.value })} className="field" placeholder="Identity document front URL" /><input value={onboarding.adminIdBackUrl} onChange={event => setOnboarding({ ...onboarding, adminIdBackUrl: event.target.value })} className="field" placeholder="Identity document back URL (optional)" /><input required value={onboarding.proofOfAddressUrl} onChange={event => setOnboarding({ ...onboarding, proofOfAddressUrl: event.target.value })} className="field sm:col-span-2" placeholder="Proof of school address URL" /><div className="sm:col-span-2 text-xs text-white/45">Identity documents and proof-of-address links must be secure, provider-accessible URLs. Brails will make the final KYB decision.</div><button className="flex items-center justify-center gap-2 rounded-xl bg-cyan-300 px-4 py-3 font-bold text-[#071018] sm:col-span-2" disabled={loading}>{loading ? 'Submitting to Brails...' : 'Submit complete KYB application to Brails'}</button></form></section>}
      {onboardingComplete && <section className="mb-8 rounded-2xl border border-amber-300/25 bg-amber-300/10 p-5"><div className="flex items-start gap-3"><ShieldCheck className="mt-0.5 text-amber-200" /><div><h2 className="font-bold text-amber-100">Step 2: KYB review in progress</h2><p className="mt-1 text-sm text-amber-100/65">Your school application is on the platform. Proxim will review the school registration and authorized administrator before master accounts and collections setup are enabled.</p><div className="mt-4 inline-flex rounded-xl border border-amber-200/30 px-4 py-2 text-sm font-semibold text-amber-100">Application submitted · KYB review</div></div></div></section>}
      <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{metrics.map(metric => <div key={metric.label} className="rounded-2xl border border-white/10 bg-[#0c1b25] p-5"><div className={metric.tone}>{metric.icon}</div><div className="mt-5 text-3xl font-bold">{metric.value}</div><div className="mt-1 text-sm text-white/45">{metric.label}</div></div>)}</div>
      <div className="mt-8 grid gap-6 lg:grid-cols-[1.4fr_1fr]">
        <section className="rounded-2xl border border-white/10 bg-[#0c1b25] p-6"><div className="mb-5 flex items-center justify-between"><div><h2 className="text-lg font-bold">Collections setup</h2><p className="mt-1 text-sm text-white/45">Fees published for each class.</p></div><WalletCards className="text-cyan-300" size={22} /></div>{overview?.fees?.length ? <div className="space-y-3">{overview.fees.map((fee, index) => <div key={`${fee.name}-${index}`} className="flex items-center justify-between border-t border-white/10 pt-3"><div><div className="font-semibold">{fee.name}</div><div className="text-xs text-white/45">Student payment records use the school master account</div></div><div className="font-semibold text-cyan-200">{fee.currency} {Number(fee.amount).toLocaleString()}</div></div>)}</div> : <div className="rounded-xl border border-dashed border-white/15 p-8 text-center text-sm text-white/45">No fee schedules connected yet.</div>}</section>
        <section className="rounded-2xl border border-white/10 bg-[#0c1b25] p-6"><div className="mb-5 flex items-center justify-between"><div><h2 className="text-lg font-bold">Reserve policy</h2><p className="mt-1 text-sm text-white/45">Applied after confirmed payments.</p></div><BarChart3 className="text-amber-300" size={22} /></div>{overview?.savingsPolicies?.length ? overview.savingsPolicies.map(policy => <div key={policy.name} className="flex items-center justify-between rounded-xl bg-white/[0.04] p-4"><div><div className="font-semibold">{policy.name}</div><div className="text-xs text-white/45">{policy.currency} · {policy.status.toLowerCase()}</div></div><div className="text-xl font-bold text-amber-200">{policy.percentage}%</div></div>) : <div className="rounded-xl border border-dashed border-white/15 p-8 text-center text-sm text-white/45">Configure a savings rule after connecting a school entity.</div>}<div className="mt-5 flex items-center gap-2 text-xs text-emerald-300"><CheckCircle2 size={14} /> Payment completion stays independent from reserve processing.</div></section>
      </div>
    </main>
  </div>;
};
